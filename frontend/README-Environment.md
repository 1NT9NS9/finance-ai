# 🌍 Environment Configuration System

## 🎯 Что это?

Новая система конфигурации, где **вы устанавливаете API ключ один раз**, и он автоматически применяется во всех модулях приложения.

## 🚀 Быстрый старт

### 1️⃣ Откройте Setup Page
```
http://localhost:5000/setup.html
```

### 2️⃣ Установите API ключ
1. Получите ключ на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Вставьте в поле "Gemini API Key"
3. Нажмите "Сохранить ключ"
4. **Готово!** API ключ применится везде автоматически

### 3️⃣ Проверьте работу
- Нажмите "Тестировать" на setup.html
- Или откройте чат и отправьте сообщение на `http://localhost:5000/chat.html`

---

## 📁 Файловая структура

```
📦 Environment System
├── 🌍 environment.js    # Главный файл конфигурации
├── 🔧 setup.html        # Удобный интерфейс настройки
├── ⚙️ config.js         # Загружает настройки из Environment
├── 🤖 gemini-api.js     # Использует настройки из Environment
└── 🧪 ai-test.html      # Работает с Environment

# Обслуживание статики: эти файлы обслуживаются Flask-сервером backend по адресу http://localhost:5000/
```

---

## 🔄 Как это работает

### Старая система:
```javascript
// Нужно было менять в каждом файле
Config.api.gemini.apiKey = 'ключ';
GeminiAI.setApiKey('ключ');
// И так в каждом модуле...
```

### Новая система:
```javascript
// Устанавливаете один раз
Environment.set('GEMINI_API_KEY', 'ключ');

// Автоматически обновляется везде:
// ✅ Config.api.gemini.apiKey
// ✅ GeminiAI.apiKey  
// ✅ Все другие модули
```

---

## ⚙️ Основные настройки

### 🔑 API Configuration
```javascript
GEMINI_API_KEY: 'ваш_ключ_здесь'     // Главный API ключ
GEMINI_MODEL: 'gemini-2.5-flash-lite'     // Модель AI
API_TIMEOUT: 30000                   // Таймаут (30 сек)
API_MAX_RETRIES: 3                   // Количество повторов
```

### 📊 User Limits
```javascript
MESSAGES_PER_DAY: 10                 // Лимит сообщений в день
MAX_MESSAGE_LENGTH: 1000             // Макс длина сообщения
MAX_CONVERSATION_HISTORY: 20         // Размер истории чата
```

### 🎛️ Feature Flags
```javascript
ENABLE_AI_RESPONSES: true            // Включить AI ответы
ENABLE_USER_REGISTRATION: true       // Регистрация пользователей
ENABLE_PAYWALL: true                 // Система подписки
ENABLE_DEBUG_MODE: true              // Режим отладки
```

### 🔒 Security Settings
```javascript
MAX_LOGIN_ATTEMPTS: 5                // Попытки входа
SESSION_TIMEOUT: 86400000            // Время сессии (24ч)
ADMIN_PASSWORD: '0000'               // Пароль админа
```

---

## 🛠️ Способы настройки

### 🎯 Способ 1: Setup Page (Рекомендуется)
1. Откройте `setup.html`
2. Используйте удобный интерфейс
3. Все изменения применяются мгновенно

### 📝 Способ 2: Редактирование файла
```javascript
// В файле environment.js
GEMINI_API_KEY: 'ваш_реальный_ключ_здесь'
```

### 💻 Способ 3: Консоль браузера
```javascript
// Откройте консоль (F12) и выполните:
Environment.set('GEMINI_API_KEY', 'ваш_ключ');
Environment.set('MESSAGES_PER_DAY', 20);
Environment.set('ENABLE_DEBUG_MODE', false);
```

---

## 🔍 Диагностика и проверка

### Проверить статус системы:
```javascript
Environment.validate()              // Проверка конфигурации
Environment.getStatus()             // Полный статус системы
Environment.export()                // Экспорт настроек
```

### Проверить API:
```javascript
GeminiAI.isConfigured()             // API ключ установлен?
GeminiAI.healthCheck()              // Тест соединения с API
```

### Посмотреть текущие настройки:
```javascript
console.log(Environment.GEMINI_API_KEY);
console.log(Environment.MESSAGES_PER_DAY);
console.log(Environment.ENABLE_AI_RESPONSES);
```

---

## 🚨 Решение проблем

### ❌ "API ключ не настроен"
```javascript
// Проверьте:
console.log(Environment.GEMINI_API_KEY);

// Если показывает 'YOUR_GEMINI_API_KEY_HERE', установите реальный ключ:
Environment.set('GEMINI_API_KEY', 'ваш_реальный_ключ');
```

### ❌ "Модули не обновляются"
```javascript
// Принудительно обновите модули:
Environment.applyToModules();

// Или перезагрузите страницу
location.reload();
```

### ❌ "Настройки не сохраняются"
```javascript
// Проверьте порядок загрузки скриптов в HTML:
// 1. environment.js (первым!)
// 2. config.js
// 3. gemini-api.js
// 4. остальные модули
```

---

## 🎨 Кастомизация

### Добавить новую настройку:
```javascript
// В environment.js
const Environment = {
    // ... существующие настройки
    MY_NEW_SETTING: 'значение',
    
    // Добавить в applyToModules():
    applyToModules() {
        // ... существующий код
        if (window.MyModule) {
            window.MyModule.setting = this.MY_NEW_SETTING;
        }
    }
};
```

### Добавить валидацию:
```javascript
// В environment.js в методе validate()
validate() {
    const issues = [];
    
    // ... существующие проверки
    
    if (!this.MY_NEW_SETTING) {
        issues.push('❌ MY_NEW_SETTING not configured');
    }
    
    return { valid: issues.length === 0, issues };
}
```

---

## 📈 Преимущества новой системы

### ✅ Централизация
- Все настройки в одном месте
- Легко найти и изменить любую настройку
- Единый источник истины

### ✅ Автоматизация
- Изменения применяются автоматически
- Не нужно обновлять каждый модуль вручную
- Синхронизация между всеми компонентами

### ✅ Удобство
- Графический интерфейс для настройки
- Валидация настроек
- Экспорт/импорт конфигурации

### ✅ Безопасность
- Скрытие чувствительных данных
- Валидация входных данных
- Контроль доступа к настройкам

---

## 🎉 Заключение

Теперь вы можете:
1. **Установить API ключ один раз** в `setup.html`
2. **Он автоматически применится везде** - в чате, тестах, всех модулях
3. **Легко управлять всеми настройками** через удобный интерфейс
4. **Не беспокоиться о синхронизации** между модулями

**Ваша AI-интеграция теперь настраивается в один клик! 🚀**