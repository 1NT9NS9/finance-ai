# 🚀 Настройка AI Studio (Gemini API)

## 📋 Быстрый старт

### 1. 🔑 Получить API ключ

1. Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Войдите в аккаунт Google
3. Нажмите **"Create API Key"**
4. Скопируйте полученный ключ

### 2. ⚙️ Настроить API ключ

**🎯 Рекомендуемый способ: Через Setup Page**
1. Откройте `setup.html` в браузере
2. Вставьте API ключ в поле "Gemini API Key"
3. Нажмите "Сохранить ключ"
4. API ключ автоматически применится везде!

**Вариант 1: Через файл environment.js**
```javascript
// В файле environment.js найдите строку:
GEMINI_API_KEY: 'YOUR_GEMINI_API_KEY_HERE'

// Замените на ваш ключ:
GEMINI_API_KEY: 'ВАШ_РЕАЛЬНЫЙ_API_КЛЮЧ_ЗДЕСЬ'
```

**Вариант 2: Через браузерную консоль**
```javascript
// Откройте консоль браузера (F12) и выполните:
window.Environment.set('GEMINI_API_KEY', 'ВАШ_РЕАЛЬНЫЙ_API_КЛЮЧ_ЗДЕСЬ');
```

### 3. ✅ Проверить работоспособность

1. Откройте чат на сайте
2. Отправьте сообщение "Привет"
3. Должен прийти ответ от AI (не стандартный шаблон)

---

## 🔧 Подробная настройка

### 📁 Структура файлов

```
project/
├── environment.js      # 🌍 Центральная конфигурация (НОВОЕ!)
├── config.js          # Основные настройки
├── gemini-api.js       # API интеграция с Gemini
├── setup.html         # 🔧 Страница настройки (НОВОЕ!)
├── ai-test.html       # 🧪 Тестирование AI
├── chat.html          # Страница чата (обновлена)
└── SETUP.md           # Этот файл
```

### 🌍 Настройки в environment.js (Главный файл конфигурации)

```javascript
const Environment = {
    // 🔑 API Configuration
    GEMINI_API_KEY: 'ВАШ_КЛЮЧ_ЗДЕСЬ',           // Главный API ключ
    GEMINI_MODEL: 'gemini-2.5-flash-lite',
    API_TIMEOUT: 30000,                         // 30 секунд
    API_MAX_RETRIES: 3,                         // 3 попытки
    
    // 📊 User Limits
    MESSAGES_PER_DAY: 10,                       // Лимит для пользователей
    MAX_MESSAGE_LENGTH: 1000,                   // Максимальная длина сообщения
    MAX_CONVERSATION_HISTORY: 20,               // История разговора
    
    // 🎛️ Feature Flags
    ENABLE_AI_RESPONSES: true,                  // Включить AI
    ENABLE_USER_REGISTRATION: true,             // Регистрация
    ENABLE_PAYWALL: true,                       // Подписка
    ENABLE_DEBUG_MODE: true,                    // Отладка
    
    // 🔒 Security
    MAX_LOGIN_ATTEMPTS: 5,                      // Попытки входа
    SESSION_TIMEOUT: 86400000,                  // 24 часа
};
```

**🎯 Преимущества новой системы:**
- ✅ **Один файл** - все настройки в одном месте
- ✅ **Автоматическое обновление** - изменения применяются везде
- ✅ **Валидация** - проверка корректности настроек
- ✅ **Удобный интерфейс** - настройка через setup.html

### 🤖 Функции AI

#### Основные возможности:
- ✅ **Ответы на русском языке**
- ✅ **Специализация на инвестициях**
- ✅ **Контекст разговора** (помнит историю)
- ✅ **Fallback ответы** (если AI недоступен)
- ✅ **Ограничения по сообщениям**

#### Специальные методы:
```javascript
// Инвестиционные советы с контекстом портфеля
await GeminiAI.getInvestmentAdvice(message, portfolioData);

// Анализ рынка
await GeminiAI.getMarketAnalysis('SBER', 'current');

// Проверка работоспособности
await GeminiAI.healthCheck();
```

---

## 🛠️ Диагностика проблем

### ❌ Проблема: AI не отвечает

**Проверьте:**
1. ✅ API ключ установлен правильно
2. ✅ Есть подключение к интернету
3. ✅ Нет ошибок в консоли браузера (F12)

**Решение:**
```javascript
// Проверить статус API:
window.GeminiAI.healthCheck().then(console.log);

// Проверить ключ:
console.log(window.GeminiAI.isConfigured());
```

### ❌ Проблема: Ошибка API key

**Симптомы:**
- Сообщение: "API ключ не настроен"
- В консоли: "Gemini API key not configured"

**Решение:**
1. Получите новый ключ на [AI Studio](https://makersuite.google.com/app/apikey)
2. Обновите config.js
3. Перезагрузите страницу

### ❌ Проблема: Лимит запросов

**Симптомы:**
- Сообщение: "Превышен лимит запросов"
- HTTP 429 ошибка

**Решение:**
- Подождите несколько минут
- Проверьте квоты в Google Cloud Console

### ❌ Проблема: Fallback ответы

**Симптомы:**
- Ответы выглядят как шаблоны
- Нет персонализации

**Это означает:**
- API не настроен или недоступен
- Используются резервные ответы
- Настройте API ключ для полной функциональности

---

## 📊 Мониторинг использования

### 📈 Статистика API

```javascript
// Проверить количество запросов пользователя
MessageLimitManager.getTodaysMessageCount();

// Проверить оставшиеся сообщения
MessageLimitManager.getRemainingMessages();

// История разговора
ConversationManager.getConversationHistory();
```

### 🔍 Логирование

В режиме разработки все API вызовы логируются в консоль:
- ✅ Запросы к Gemini API
- ✅ Ответы от AI
- ✅ Ошибки и повторные попытки
- ✅ Использование fallback ответов

---

## 🚦 Тестирование

### 🧪 Базовый тест

1. **Зарегистрируйтесь** на сайте
2. **Откройте чат**
3. **Отправьте:** "Расскажи про акции Сбербанка"
4. **Ожидаемый результат:** Развернутый ответ про инвестиции

### 🧪 Тест лимитов

1. **Отправьте 9 сообщений** подряд
2. **10-е сообщение:** должно показать "Осталось 0 сообщений"
3. **11-е сообщение:** должно вызвать paywall

### 🧪 Тест fallback

1. **Отключите интернет** или установите неверный API ключ
2. **Отправьте сообщение**
3. **Ожидаемый результат:** Шаблонный ответ (fallback)

---

## ⚡ Быстрые команды

```javascript
// Установить API ключ
GeminiAI.setApiKey('ваш_ключ');

// Проверить статус
GeminiAI.healthCheck();

// Очистить историю чата
ConversationManager.clearHistory();

// Сбросить лимиты (для тестирования)
localStorage.clear();
```

---

## 📞 Поддержка

Если что-то не работает:

1. **Проверьте консоль браузера** (F12 → Console)
2. **Убедитесь в правильности API ключа**
3. **Проверьте подключение к интернету**
4. **Попробуйте перезагрузить страницу**

**Готово! Ваш AI помощник для инвестиций настроен! 🎉**